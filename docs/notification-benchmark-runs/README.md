# 通知压测原始记录

## Synchronous run: 20260913-194011-4bb7d47c

同步报告使用 `20260913-194011-4bb7d47c/` 中的点赞、收藏、评论三组结果。该轮在开始前确认 Outbox 无 pending 事件，RabbitMQ 主队列、重试队列和死信队列均为 0。

目录保存 `machine.json`、每个场景的 k6 summary、完整 k6 输出、应用/MySQL/Redis/RabbitMQ 资源采样和运行参数。

## Final Outbox run: 20260913-200150-fd4d4059

Outbox 报告使用 `20260913-200150-fd4d4059/` 三组结果。此轮覆盖最终的 mandatory publish Return 检查；与同步轮使用相同机器、k6 脚本、20 VU、30 秒、接口、JWT 鉴权与种子规则。三场景合计 1,502 个 Outbox 事件均发布完成，且有 1,502 条通知记录。

该目录同样保存机器信息、k6 JSON/输出、运行参数和逐场景资源采样。可使用项目 README 中的 PowerShell 命令重现。

更早的 `20260913-192132-2e83c8e3` 是脚本首次调试，未得到完整 P99；`20260913-192627-b6ec9a92` 在 publisher 逐条轮询的旧实现下中断；`20260913-193217-f63d1963` 完成但开始时仍有调试队列积压。这些目录保留作过程记录，不纳入最终报告数据。
